[pytest]
addopts = -rfE
markers =
    slow: mark a test as slow.
    unsafe: the test writes to the database

# filterwarnings =
#     ignore:.*'_app_ctx_stack' is deprecated and will be removed in Flask 2.4.
